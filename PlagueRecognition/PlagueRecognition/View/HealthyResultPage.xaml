<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlagueRecognition.View.HealthyResultPage"
             BackgroundColor="{StaticResource Green}"
             NavigationPage.HasNavigationBar="False">

    <ContentPage.Content>
        <Grid HorizontalOptions="Center"
              VerticalOptions="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image Source="leaf.png"
                   Style="{StaticResource PrimaryImage}"
                   Grid.Row="0"/>
            <Label Text="Eba! Planta saudável"
                   Style="{StaticResource PrimaryLabel}"
                   Margin="0,20,0,0"
                   Grid.Row="1"/>
            <Label Text="99% de probabilidade"
                   Style="{StaticResource SecondLabel}"
                   Margin="0,0,0,80"
                   Grid.Row="2"/>
            <Button Text="Reescanear"
                    Style="{StaticResource BaseButton}"
                    Grid.Row="3"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>
